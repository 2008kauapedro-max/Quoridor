/* =============================================================
   The Rage Arena — Configuração (MODELO / EXEMPLO)
   -------------------------------------------------------------
   ⚠️ Este arquivo é só o EXEMPLO. Quem o jogo lê de verdade é:
      js/config.js   (mesmo formato, mesmos campos)

   Como pegar suas chaves GRÁTIS (plano free do Supabase):
   1) supabase.com → New project (gratuito)
   2) Dashboard do projeto → Settings → API
   3) Copie:
      • Project URL  → cole em SUPABASE_URL
      • anon public  → cole em SUPABASE_ANON_KEY
   4) Salve e suba o projeto na Vercel. Pronto.

   🔒 A chave "anon" é pública de propósito (o resto da segurança
      vem das políticas RLS do banco, que entregaremos depois).
   ============================================================= */

export const CONFIG = {
  /* Exemplo: "https://xyzabcd1234.supabase.co" */
  SUPABASE_URL: "",

  /* Exemplo: "eyJhbGciOiJIUzI1NiIsInR5cCI6..." (chave anon) */
  SUPABASE_ANON_KEY: ""
};